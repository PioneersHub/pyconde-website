[model]
name = Redirect

[fields.target]
label = Redirect Target
type = string
description = Target is of type 'string' to allow relative paths. Converted to url in the template.

[fields.status]
label = HTTP status (informational; HTML redirects can't actually return 301)
type = integer
default = 301

[fields.note]
label = Why this redirect exists
type = string

[fields._marker]
label = Generator marker (do not edit)
type = string
