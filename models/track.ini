[model]
name = track
label = Track
label_field = name
hidden = yes

[fields.name]
label = Track name (as in Pretalx)
type = string

[fields.slug]
label = URL slug
type = string

[fields.year]
label = Edition year (empty for current edition)
type = string

[fields.talk_count]
label = Cached talk count
type = integer
