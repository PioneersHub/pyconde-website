# PyConDE Contact Form Backend Configuration
# ============================================
# This file contains environment variables for local development.
# For AWS Lambda deployment, these are configured via SAM template parameters.

# reCAPTCHA Configuration
# Get these from https://www.google.com/recaptcha/admin
RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# Email Configuration
EMAIL_RECIPIENT=info26@pycon.de
EMAIL_SENDER=noreply@pycon.de
EMAIL_SUBJECT_PREFIX=[Website]

# Mailgun Configuration
# Get these from your Mailgun dashboard: https://app.mailgun.com/
MAILGUN_API_KEY=your_mailgun_api_key_here
MAILGUN_DOMAIN=your_mailgun_domain.com
# Use https://api.mailgun.net/v3 for US region
# Use https://api.eu.mailgun.net/v3 for EU region
MAILGUN_API_BASE_URL=https://api.mailgun.net/v3

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://2026.pycon.de,https://pycon.de,http://localhost:5001,http://127.0.0.1:5001

# Debug Mode (set to False in production)
DEBUG=False

# Lambda Deployment Notes:
# - FastAPI docs (/docs, /redoc) disabled in production (only available when DEBUG=True)
# - WAF rules protect against DDoS, SQL injection, XSS attacks
# - For AWS Lambda: Configure environment variables in template.yaml or template-secure.yaml
# - For local testing: Use this .env file or AWS SAM local with --env-vars
