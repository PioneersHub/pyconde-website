display: true
# ──────────────────────────────────────────────────────────────
# Front-page Featured Videos section
# ──────────────────────────────────────────────────────────────#
# Hand-curated video shelf — technically identical to the
# latest_videos section, but the cards are the pretalx sessions
# listed below, in this order.
#
# `sessions` entries match the `code:` field of talk pages
# (current edition and every /archive/{year}). Sessions without a
# published recording (no youtube_id, or do_not_record) are
# skipped silently.
#
# Whether the section renders at all is set in
# databags/frontpage/config.yaml.
# ──────────────────────────────────────────────────────────────

section_title: "Featured videos"
sessions:
  - BFL7MQ   # Keynote — From Scratch to Scale
  - CMDHUN   # Keynote — "Honey, I vibe coded some crypto"
  - LPUC9T   # Keynote — The Multimodal Era of Machine Learning
  - LSJ3CN   # Keynote — A View of Sovereignty from The Cloud
  - 9CYWET   # Fireside Chat — Stop Waiting, Start Shipping
